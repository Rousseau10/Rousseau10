@model R10.Models.Test_DateTime2_GetDate

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

<script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)
    <fieldset>
        <legend>Test_DateTime2_GetDate</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.Id_PK_not_seed_nullable)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Id_PK_not_seed_nullable)
            @Html.ValidationMessageFor(model => model.Id_PK_not_seed_nullable)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.dt_datetime2_GetDate_nullable)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.dt_datetime2_GetDate_nullable)
            @Html.ValidationMessageFor(model => model.dt_datetime2_GetDate_nullable)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.comments_nulable)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.comments_nulable)
            @Html.ValidationMessageFor(model => model.comments_nulable)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
